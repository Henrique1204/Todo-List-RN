import { StyleSheet } from 'react-native';

const Styles = StyleSheet.create({
	container: {
		flex: 1,
		alignItems: 'center',
	},
	logo: {
		width: 110,
		height: 32,
		marginTop: 24,
		marginBottom: 40,
	},
});

export default Styles;
