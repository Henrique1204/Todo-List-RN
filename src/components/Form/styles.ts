import { StyleSheet } from 'react-native';

const Styles = StyleSheet.create({
	container: {
		width: '100%',
		paddingLeft: 24,
		paddingRight: 24,

		flexDirection: 'row',
		gap: 4,

		position: 'relative',
		zIndex: 1,
	},
});

export default Styles;
